// apps/ms-usuarios/prisma/schema.prisma

generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["driverAdapters"]
  // output       = "../node_modules/.prisma/client" // Opcional, si te da problemas de rutas
}

datasource db {
  provider = "postgresql"
  // ¡OJO! Aquí YA NO VA la línea 'url = env("DATABASE_URL")'
  // Se deja solo el provider.
}

model User {
  // ... resto de tu modelo igual
  id        String   @id @default(uuid())
  // etc...
}